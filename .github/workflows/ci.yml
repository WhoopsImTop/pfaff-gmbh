on:
  push:
    branches:
      - main
      
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: Use Node.js 16
      uses: actions/setup-node@v2
      with:
        node-version: '16'
      
    - name: 🔨 Build Project
      run: |
        npm install
        npm run generate

    - name: 📦 Zip files
      run: |
        zip -r website.zip dist/

    - name: 🚀 Deploy via FTP
      uses: tomasbkk/action-ftp-upload@v1.0
      with:
        host: ${{ secrets.TESTHOST }}
        user: ${{ secrets.TESTUSERNAME }}
        password: ${{ secrets.TESTPASSWORD }}
        src: './website.zip'
        dest: /

    - name: 📦 Unzip files on server
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.TESTHOST }}
        username: ${{ secrets.TESTUSERNAME }}
        password: ${{ secrets.TESTPASSWORD }}
        script: |
          unzip -o website.zip -d /pfaff-web-2023-nuxt/