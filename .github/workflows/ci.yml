on:
  push:
    branches:
      - main
      
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2

    - name: Use Node.js 16
      uses: actions/setup-node@v2
      with:
        node-version: '16'
      
    - name: 🔨 Build Project
      run: |
        npm install
        npm run generate
            
    - name: 📂 Sync Files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ssh.strato.de
        username: khri8@pfaffgmbh.com
        password: ${{ secrets.passord }}
        server-dir: pfaff-web-2023-nuxt/
        local-dir: ./dist/
        protocol: ftps
        port: 22